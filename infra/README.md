# Infrastructure

Local infrastructure will likely include:

- MongoDB for LibreChat state
- Postgres with pgvector for retrieval and application data
- Redis for queueing and caching
- object storage such as MinIO
- Langfuse for tracing and evals

Add compose files and bootstrap scripts here as the first runnable environment is assembled.
